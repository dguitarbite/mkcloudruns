---
proposals:
- barclamp: nfs_client
  name: novastuff
  attributes:
    exports:
      nova:
        nfs_server: crowbar.c@@i.cloud.suse.de
        export: "/var/lib/nova/instances"
        mount_path: "/var/lib/nova/instances"
        mount_options:
        - ''
  deployment:
    elements:
      nfs-client:
      - "@@compute1@@"
      - "@@compute2@@"

